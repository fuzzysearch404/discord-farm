'CREATE TABLE IF NOT EXISTS users(id decimal PRIMARY KEY, guildid bigint NOT NULL, userid bigint NOT NULL, xp bigint NOT NULL, money bigint NOT NULL, gems integer NOT NULL, tiles integer NOT NULL, usedtiles integer NOT NULL);'
'CREATE TABLE IF NOT EXISTS inventory(itemid smallint NOT NULL, userid decimal NOT NULL, amount integer NOT NULL);'
%sql CREATE TABLE IF NOT EXISTS planted(id SERIAL PRIMARY KEY, itemid smallint NOT NULL, userid decimal NOT NULL, amount integer NOT NULL, ends timestamp NOT NULL, dies timestamp NOT NULL, robbed boolean NOT NULL);
